# 24 验证矩阵

| 改动类型          | requires_retest | 最低验证             | 推荐验证                            | 当前状态                                   |
| ----------------- | --------------- | -------------------- | ----------------------------------- | ------------------------------------------ |
| 只改文档          | 否              | handoff 自检         | 人工读 00/01/23/29/30               | 本次自检脚本误扫依赖目录，需修脚本         |
| Auth / 登录       | 是              | typecheck + 登录验证 | refresh / 40100 / logout / 权限冒烟 | #51 已改历史代码，需补浏览器专项           |
| Router / 菜单     | 是              | build + 页面跳转     | 登录后菜单与权限过滤                | IA v3 七组菜单已于 #84 后逐 PR 冒烟(见 17) |
| 业务 API 页面     | 是              | typecheck + 单页列表 | 后端 seed + 浏览器 CRUD 冒烟        | #82~#93 逐 PR :8849+live 冒烟(见 17)       |
| 字典主从布局      | 是              | 页面加载 + 类型切换  | 字典类型 CRUD + 条目树表 CRUD       | #71/#72 已合入，待浏览器复核               |
| srvf-kit 原语迁移 | 是              | build + 试点页验证   | 多页面视觉/交互回归                 | #74~#80 已合入，待冒烟                     |
| 依赖              | 是              | lock diff + build    | 安全审查 + 回滚方案                 | 本次未涉及                                 |

## 当前验证结论

- 直接类型检查：PASS。
- 直接生产构建：PASS。
- pnpm 脚本入口：BLOCKED（环境/依赖状态检查问题）。
- handoff 自检：FAIL_NOISY（脚本扫描范围问题）。
- 浏览器/dev：NOT_RUN。

当前状态只能写为 `BUILD_PASS`。
