# 平台本地运行端口号
VITE_PORT = 8848

# 是否隐藏首页 隐藏 true 不隐藏 false （勿删除，VITE_HIDE_HOME只需在.env文件配置）
VITE_HIDE_HOME = false

# 是否启用多租户 启用 true 不启用 false （勿删除，VITE_ENABLE_TENANT只需在.env文件配置）
# PR-2：starter 第一阶段一律禁用多租户运行入口（裁决 1）；源码保留作为参考
VITE_ENABLE_TENANT = false